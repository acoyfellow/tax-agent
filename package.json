{
  "name": "tax-agent",
  "version": "1.0.0",
  "description": "AI-powered tax filing agent on Cloudflare Workers — validates your data with Workers AI, files via Column Tax",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write 'src/**/*.ts'",
    "lint": "prettier --check 'src/**/*.ts'"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "wrangler": "^3.109.0",
    "prettier": "^3.5.0",
    "@cloudflare/vitest-pool-workers": "^0.8.0"
  }
}
