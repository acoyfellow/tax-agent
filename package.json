{
  "name": "tax-agent",
  "version": "2.0.0",
  "description": "AI-powered tax form agent on Cloudflare Workers — validates with Workers AI, files via TaxBandits",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "prepare": "lefthook install",
    "check": "tsc --noEmit && prettier --check 'src/**/*.ts'",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write 'src/**/*.ts'",
    "lint": "prettier --check 'src/**/*.ts'"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.0",
    "@cloudflare/workers-types": "^4.20250214.0",
    "lefthook": "^2.1.1",
    "prettier": "^3.5.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.65.0"
  }
}
